#include <bits/stdc++.h>
using namespace std;
int n;

int main() {
    cin>>n;
    cout<<n / 10<<" "<<n % 10;
}

//task
/*
    A simple task
    task. Write a program that numbers the given two-digit numbers
    to output separately, separated by a space.
    Input data: a single integer n (10 <= n < 100).
    Output data: two numbers separated by space.
*/